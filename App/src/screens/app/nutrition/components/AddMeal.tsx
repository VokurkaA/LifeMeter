import { View } from "react-native";
import { Text } from "@/components/Text";
import { useStore } from "@/contexts/useStore";

export default function AddMeal() {
    // const { createUserMeal } = useStore()
    return (
        <View>
            <Text>Add Meal Component</Text>
        </View>
    );
}